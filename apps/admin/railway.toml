# Railway Configuration for Plane Admin

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.admin"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
