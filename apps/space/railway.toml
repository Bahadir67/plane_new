# Railway Configuration for Plane Space Service

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.space"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
