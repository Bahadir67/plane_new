# Railway Configuration for Plane Live (Real-time) Service

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.live"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
