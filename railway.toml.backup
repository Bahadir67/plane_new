# Railway Configuration for Plane Turkish Edition
# This file configures the main web application
# Other services (api, admin, space, live) should be deployed as separate Railway services

[build]
builder = "dockerfile"
dockerfilePath = "apps/web/Dockerfile.web"

[deploy]
# healthcheckPath = "/"
# healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
